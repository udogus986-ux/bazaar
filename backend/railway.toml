[build]
builder = "nixpacks"
buildCommand = "npm install && node node_modules/prisma/build/index.js generate && npm run build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
